# Physics Lab 桌面应用

为中学物理老师生成、修改、演示交互式物理演示 HTML 的 Windows 桌面应用。老师对话式驱动 agent 产出演示，预览并投影上课。

## Language

**工作目录 (workspace)**:
老师启动应用时指定的目录，存放全部演示文档及其资源；整个目录可拷贝到任意电脑直接用浏览器打开。
_Avoid_: 项目目录、素材库、数据目录

**演示文档 (demo)**:
工作目录中的一个演示 HTML 文件；每个文件对应 `.pi-sessions/` 中一个独立的 agent session。所有演示共享工作目录下的一份 `lib/`。
_Avoid_: 仿真、模拟、动画、页面

**会话 (session)**:
一个演示文档专属的 agent 对话历史，存于工作目录 `.pi-sessions/`，随工作目录整体迁移。
_Avoid_: 聊天记录、对话文件

**演示模式 (presentation)**:
应用内全屏渲染当前演示文档的展示状态，供课堂投影。
_Avoid_: 全屏模式、放映

**演示 HTML (demo html)**:
由 physics-lab-skill 按模板生成的、自包含离线可运行的交互式物理演示页面（依赖同目录 `lib/`）。
_Avoid_: 网页、课件

**主模型 (main model)**:
执行 agent 工作（对话、推导、生成 HTML）的模型；可为纯文本（如 DeepSeek）或多模态。
_Avoid_: 生成模型、对话模型

**视觉模型 (vision model)**:
负责理解图片的模型（OCR 通道）；当主模型不支持图片时，图片先经视觉模型转为文本/公式描述再交给主模型。可独立配置 Key，也可与主模型同 Key。
_Avoid_: OCR 模型、图片模型

**OCR 通道 (ocr channel)**:
应用层对图片附件的处理路径：主模型支持视觉则图片直通，否则调视觉模型提取内容后以文本进入会话。
_Avoid_: 图片识别、读图

**题目 (problem)**:
带明确设问/选项/答案的物理题；agent 需先索要或核对答案，推导结论需用户确认。
_Avoid_: 习题、考题

**物理过程 (process)**:
无设问的物理现象/装置描述（如"绳环从高处下落"）；agent 简述理解即可，不需答案确认。
_Avoid_: 现象、场景
